export const RespAttrOptions = ['headers', 'cookies', 'json'].map((item) => ({
  value: item,
  label: item,
}));

export const AssertOptions = ['equals', 'contains'].map((item) => ({
  value: item,
  label: item,
}));
